[project]
name = "greeting-dinesh58"
version = "0.1.1"
description = "A simple Python CLI tool to greet anyone."
authors = [{name = "Dinesh"}]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
greet = "greeting:main"

[tool.hatch.build.targets.wheel]
packages = ["greeting"]
